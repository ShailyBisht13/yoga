# Task: Use logo.png as Logo and hero image in the site

## Steps
- [x] 1. Move `logo.png` and `72f80c8f-f566-427b-9e07-1ae26cbc16e6.png` to `public/` folder
- [ ] 2. Update `Logo.jsx` — Replace SVG with `<img>` using `/logo.png`
- [ ] 3. Create `HeroSection.jsx` — Hero banner component using the hero image
- [ ] 4. Update `sections/index.js` — Export HeroSection
- [ ] 5. Update `HomePage.jsx` — Render HeroSection

